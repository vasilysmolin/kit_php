name: ci-cd

on:
  push:
    branches:
      - '**'
    tags:
      - '**'

jobs:
  app-make-lint:
    name: CI
    runs-on: ubuntu-latest

    steps:

      - name: Copy .env
        run: php -r "file_exists('.env') || copy('.env.example', '.env');"

      - name: Run builds
        run: docker-compose -f docker-compose.ci.yml up -d --build

      - name: Checkout code
        uses: actions/checkout@v2

      - name: Run install
        run: docker-compose -f docker-compose.ci.yml exec -T php composer install --no-interaction --ansi --no-suggest

